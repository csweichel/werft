containers:
- name: build
  image: golang:1.13-alpine
  workingDir: /workspace
  imagePullPolicy: IfNotPresent
  command:
    - sh 
    - -c
    - |
      cd /workspace
      go get -v ./...
      go build